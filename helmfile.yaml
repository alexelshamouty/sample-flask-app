releases:
  - name: "myapp"
    chart: "deploy/"
    namespace: myapp
    createNamespace: true
    values:
      - values-prod.yaml
    secrets:
      - secrets-prod.yaml
environments:
  prod:
    kubeContext: kubernetes-admin@kubernetes